# Черепашка у вікні із заданими координатами
import turtle

window = turtle.Screen()
window.title("Черепашка у вікні із заданими координатами")
window.bgcolor("yellow")
window.setup(width=800, height=400, startx=100, starty=100)

window.mainloop()